<Window x:Class="GSD.Views.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:GSD" xmlns:viewModels="clr-namespace:GSD.ViewModels" mc:Ignorable="d" Title="MainWindow" Height="350" Width="525">
	<Window.DataContext>
		<viewModels:MainViewModel />
	</Window.DataContext>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>

		<DockPanel Grid.Column="0">
			<ListView ItemsSource="{Binding Projects}">
				<ListView.ItemTemplate>
					<DataTemplate DataType="viewModels:ProjectViewModel">
						<TextBlock Text="{Binding Model.Name}" />
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</DockPanel>

		<DockPanel Grid.Column="1">
			<ItemsControl ItemsSource="{Binding CurrentProject.Todos}" />
		</DockPanel>

		<DockPanel Grid.Column="2">
			<ListView ItemsSource="{Binding CurrentProject.Tags}">
				<ListView.ItemTemplate>
					<DataTemplate DataType="viewModels:TagViewModel">
						<TextBlock Text="{Binding Model.Name}" />
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</DockPanel>
	</Grid>
</Window>